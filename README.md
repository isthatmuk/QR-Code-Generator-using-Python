# QR-Code-Generator-using-Python
This Python project generates QR codes from any text or URL entered by the user. Using the qrcode and Pillow libraries, it creates and saves the QR code as a PNG image. If the filename doesn't include the .png extension, it is added automatically, making the program simple, beginner-friendly, and easy to use.
